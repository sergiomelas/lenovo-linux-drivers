[xxx]  MAINTAINERS
(Contains your contact info and the list of files you are responsible for).

[xxx]  Documentation/hwmon/index.rst
(The master list of documentation where you must add the name yogafan).

[xxx] Documentation/hwmon/yogafan.rst
(The specific description of your driver and the RLLag filter).

[xxx] drivers/hwmon/Kconfig
(The menu entry that allows users to select your driver during a kernel build).

[xxx] drivers/hwmon/Makefile
(The instruction that tells the compiler to build yogafan.o).

[xxx] drivers/hwmon/yogafan.c
(The actual C source code for the driver).


# 1. Stage all changes
git add drivers/hwmon/yogafan.c \            #the code(s)
        Documentation/hwmon/yogafan.rst \    #the doc(s)
        Documentation/hwmon/index.rst \      #the index
        drivers/hwmon/Kconfig \              #the kernel config (so can be aded)
        drivers/hwmon/Makefile \             #the compile script
        MAINTAINERS                          #the info of me

# 2. Update the commit message and add the vx changelog
git commit --amend

# 3. Generate the patch
git format-patch -1 -vx --stdout > v6-yogafan.patch
